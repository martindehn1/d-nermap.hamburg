{
  "name": "doenermap-hamburg",
  "private": true,
  "description": "Dönermap Hamburg — Website und gemeinsame Datenbasis für die App",
  "type": "module",
  "scripts": {
    "build:data": "node scripts/build-data.mjs",
    "build:api": "node scripts/build-api.mjs",
    "build": "npm run build:data && npm run build:api",
    "check": "node --check assets/js/doenermap-data.js && node --check assets/js/doenermap.js"
  }
}
